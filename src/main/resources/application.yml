#spring.application.name=springboot-course
#
#spring.datasource.url=jdbc:mysql://localhost:3306/test
#spring.datasource.username=root
#spring.datasource.password=Wall3t53v1ce
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.show-sql=true
#server.port=9005
#server.error.include-message=ALWAYS
notification:
  type: Message

server:
  port: 9005
  error:
    include-message: always




spring:
  kafka:
    bootstrap-servers: localhost:9092      # adjust host:port as needed
    consumer:
      properties:
        metadata.max.age.ms: 1000
#      group-id: my-group
#      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
##      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#    producer:
#      key-serializer: org.apache.kafka.common.serialization.StringSerializer
##      value-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.springframework.kafka.support.serializer.JsonDeserializer

#spring:
#  kafka:
#    consumer:
#      bootstrap-servers: http://localhost:9093
#      auto-offset-reset: earliest
#      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
#
#    producer:
#      bootstrap-servers: http://localhost:9093
#      key-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  application:
    name: springboot-course

  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: Wall3t53v1ce

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true


  mail:
    host: smtp.gmail.com
    port: 587
    username: your_email@gmail.com
    password: your_app_password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    default-encoding: UTF-8


properties:
  max-poll-records: 500
  off-set-config: latest
  max-poll-interval: 60000
  concurrency: 4

